_target_: ml_project_2_mlp.model.Homepage2VecLitModule
model_path: ${paths.model_dir}/homepage2vec/model.pt
device: ${trainer.accelerator}

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.001
  weight_decay: 0.0

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  _partial_: true
  mode: min
  factor: 0.1
  patience: 10
