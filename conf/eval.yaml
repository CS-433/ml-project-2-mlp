# @package _global_

# Default configurations (precendence in reverse order)
defaults:
  - data: crowdsourced # Don't change!
  - model: homepage2vec
  - logger: wandb
  - trainer: default
  - paths: default
  - global: default
  - _self_

# Specific for `eval.py`
run: ??? # W&B Run ID
version: best # Model version (Can be `latest`, `best` or specific version, e.g. `v0`)
wandb_ckpt: ${logger.wandb.entity}/${logger.wandb.project}/model-${run}:${version}
task_name: eval
tags: ["dev"]
