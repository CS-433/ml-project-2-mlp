# @package _global_

# Default configurations (precendence in reverse order)
defaults:
  - _self_
  - data: crowdsourced
  - model: mnist
  - logger: null
  - trainer: default
  - paths: default

# Experiment name
name: ???
task_name: eval
tags: ["dev"]
